if(TARGET application_admin_commands)
    add_subdirectory(admin_commands)
endif()
if(TARGET application_auth)
    add_subdirectory(auth)
endif()
if(TARGET application_anongame_inforeply)
    add_subdirectory(anongame_inforeply)
endif()
if(TARGET application_icon_table)
    add_subdirectory(icon_table)
endif()
if(TARGET application_tournament)
    add_subdirectory(tournament)
endif()
if(TARGET application_profile)
    add_subdirectory(profile)
endif()
if(TARGET application_chat)
    add_subdirectory(chat)
endif()
if(TARGET application_game)
    add_subdirectory(game)
endif()
if(TARGET application_moderation)
    add_subdirectory(moderation)
endif()
if(TARGET application_i18n)
    add_subdirectory(i18n)
endif()
if(TARGET application_realm)
    add_subdirectory(realm)
endif()
if(TARGET application_init)
    add_subdirectory(init)
endif()
if(TARGET application_email_management)
    add_subdirectory(email_management)
endif()
if(TARGET application_ads)
    add_subdirectory(ads)
endif()
if(TARGET application_anongame_lobby)
    add_subdirectory(anongame_lobby)
endif()
if(TARGET application_bnet_packet_pump)
    add_subdirectory(bnet_packet_pump)
endif()
add_subdirectory(ports)
if(TARGET application_social)
    add_subdirectory(social)
endif()
# ladder tests gated on application_ladder, which is intentionally not yet
# declared (the ladder use-case sources don't compile — see progress 1.10).
if(TARGET application_ladder)
    add_subdirectory(ladder)
endif()
