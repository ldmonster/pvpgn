add_subdirectory(shared)
if(TARGET domain_identity)
    add_subdirectory(identity)
endif()
if(TARGET domain_chat)
    add_subdirectory(chat)
endif()
if(TARGET domain_social)
    add_subdirectory(social)
endif()
if(TARGET domain_gameplay)
    add_subdirectory(gameplay)
endif()
if(TARGET domain_ladder)
    add_subdirectory(ladder)
endif()
if(TARGET domain_moderation)
    add_subdirectory(moderation)
endif()
if(TARGET domain_matchmaking)
    add_subdirectory(matchmaking)
endif()
if(TARGET domain_realm)
    add_subdirectory(realm)
endif()
if(TARGET domain_connection)
    add_subdirectory(connection)
endif()
if(TARGET domain_d2cs)
    add_subdirectory(d2cs)
endif()
if(TARGET domain_d2dbs)
    add_subdirectory(d2dbs)
endif()
